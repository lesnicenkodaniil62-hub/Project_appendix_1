# **Проект Банковское приложение.**
## Описание данное приложение создано с целью обучения на данный момент. 
#### ***Примечание это пример подобных приложений созданный на основе учебного материала для обучения.***

## Нововведение и функции:
###  Нововведение для пользователей
1. Была введена возможность ввести номер карты или номер счёта.
2. Была введена возможность ввести дату.
###  Нововведение технические не предназначенные для глаз пользователя 
1. Была введена функция маска для номера карты.
2. Была введена функция маска для номера счёта.
3. Была введена функция общего приёма для номера карты номера счёта от пользователя.
4. Была введена функция приёма даты от пользователя.
5. Была введена функция фильтрация список данных по ключу 'state'.
6. Была введена функция сортировки список данных по дате.
7. Была введена функция генератор номеров банковских карт.
8. Была введена функция декоратора логинов.
9. Была введена функция читает JSON-файл.
10. Была введена функция конвертирует сумму транзакции в рубли.
11. Была введена функция дату в формате ГГГГ-ММ-ДД даты и времени.
12. Была введена функция конвертированную сумму через внешний API.
13. Была введена функция логирование для файла utils.py данные о пользователях вносятся в реестр (технический процесс) в папку logs в файл utils.log.
14. Была введена функция логирование для файла masks.py данные о пользователях вносятся в реестр (технический процесс) в папку logs в файл masks.log.
15. Была введена функция чтение файла в формате csv с помощу встроенной библиотеке paiton csv. 
Она используется при работах с таблицами в paiton.
16. Была введена функция чтение файла в формате xlsx с помощу и импортированной библиотеке pandas в проект.
Это библиотеке работает с форматами документа Excel, CSV или SQL. А для отображение формате xlsx используется openpyxl.
17. Была введена функция поиск выполняется с помощью регулярных выражений.
18. Была введена функция подсчитывает количество операций для каждой категории с использованием библиотеки collections.
19. Была проделана большая работа с файлом main и была разработана сортировка. 
####  Дальнейшие функции будут описаны в последующих обновлениях.

### Примечание к функциям

Для генераторов пришлось использовать встроенную библиотеку typing для корректной работы были взяты некоторые функции Any, Dict, Generator, Iterator, List
Для декоратора пришлось использовать встроенную библиотеку typing для корректной работы были взяты некоторые функции Any, Callable, Dict, Optional, Tuple
Из встроенной библиотеки functools функции wraps для метаданных. 
Для тестирование кода декоратора я использовал встроенную системную библиотеку os, библиотеку typing для корректной работы были взяты некоторые функции Any, Generator.   
Для подключение api использовалась системные библиотеки os, datetime, а также пришлось импортировать библиотеку requests.
для тестирование api пришлось импортировать python-dotenv.

## Инструкция по настройке линтеров для данного проекта

Создайте файл с названием ".flake8" и в нём введите следующие
```
[flake8]
max-line-length = 119
ignore = E402, E251, E203
exclude = .git, .venv, __pycache__, venv

```
Вводите в файл pyproject.toml, если там не прописано, то пропишите следующие.
```
[tool.black]
# Максимальная длина строки
line-length = 119
# Файлы, которые не нужно форматировать
exclude = '''
(
  /(
      \.eggs         # Исключить несколько общих каталогов
    | \.git          # в корне проекта
    | \.hg
    | \.mypy_cache
    | \.tox
    | \.venv
    | dist
  )/
  | foo.py           # Также отдельно исключить файл с именем foo.py
                     # в корне проекта
)
'''


[tool.isort]
# максимальная длина строки
line_length = 119


[tool.mypy]
disallow_untyped_defs = true
warn_return_any = true
ignore_missing_imports = true
exclude = "venv"

```
И все зависимости что есть на данный момент будут прописаны.

## Тестирование кодов 

### 1. Проверка кода get_mask_card_number с использованием pytest

* Тест корректной карты с 16 цифрами
* Тест карты с менее чем 16 цифрами
* Тест карты с буквами
* Тест пустой строки
* Тест карты с более чем 16 цифрами
* Тест карты со спецсимволами

### 2. Проверка кода get_mask_account с использованием pytest

* Тест корректного счета
* Тест сокращённого счета состоящих из последних 4 цифр 
* Тест счета с менее чем 4 цифрами
* Тест счета с буквами
* Тест пустого счета

### 3. Проверка кода mask_account_card с использованием pytest

* Тест карты VisaVisa Classic
* Тест карты Visa Platinum
* Тест карты Visa Gold
* Тест карты MasterCard
* Тест счета
* Тест карты Maestro
* Не стандартный Тест карты МИР
* Тест строки без пробела
* Тест некорректного номера карты
* Тест некорректного номера счета

### 4. Проверка кода get_date с использованием pytest

* Тест стандартной даты
* Тест даты с другим временем
* Тест даты без миллисекунд
* Тест даты с Z в конце
* Тест первого дня года
* Тест последнего дня года

### 5. Проверка кода filter_by_state с использованием pytest

* Тест фильтрация по умолчанию (state="EXECUTED")
* Тест фильтрация по статусу "CANCELED"
* Тест пустой список
* Тест нет элементов с указанным статусом
* Тест список с отсутствующим ключом 'state'


### 6. Проверка кода sort_by_date с использованием pytest

* Тест сортировка по убыванию (по умолчанию) - сначала новые
* Тест сортировка по возрастанию - сначала старые
* Тест пустой список
* Тест список с одним элементом
* Тест элементы с одинаковыми датами
* Тест отсутствие ключа 'date' у некоторых элементов

### 7. Проверка кода filter_by_currency с использованием pytest

* Тест фильтрации транзакций по валюте USD.
* * Проверяем, что все отфильтрованные транзакции имеют валюту USD.
* Тест фильтрации транзакций по валюте RUB.
* Тест фильтрации по валюте, отсутствующей в транзакциях.
* Тест фильтрации пустого списка транзакций.
* Тест проверки свойств генератора.
* * Проверяем, что это генератор.
* * Проверяем ленивую загрузку.
* Тест обработки транзакций с некорректной структурой.

### 8. Проверка кода transaction_descriptions с использованием pytest

* Тест получения описаний для обычных транзакций.
* Тест получения описаний из пустого списка.
* Тест получения описания из одной транзакции.
* Тест получения описания из транзакции без ключа описание.
* Тест получения описаний из смешанных транзакций.
* Тест проверки свойств генератора описаний.
* * Проверяем ленивую генерацию

### 9. Проверка кода генератора card_number_generator с использованием pytest

* Тест генерации карт в небольшом диапазоне.
* Тест генерации одной карты.
* Тест генерации карт с ведущими нулями.
* Тест генерации максимального значения карты.
* Тест генерации карт при переходе через разряды.
* Тест проверки форматирования номеров карт.
* * Проверяем формат: 4 группы по 4 цифры.
* Тест ошибки при начальное значение меньше 1.
* Тест ошибки при отрицательном start.
* Тест ошибки при end превышающем максимальное значение.
* Тест ошибки когда начальное значение больше конечного.
* Тест генерации большого диапазона карт (проверка количества).
* Тест проверки свойств генератора.
* * Проверяем ленивую генерацию.
* * Проверяем, что генератор исчерпан.

### 10. Проверка кода декоратора log с использованием pytest

* Тест успешного выполнения функции с указанием файла логов. 
* Тест успешного выполнения функции без указания файла (вывод в консоль).
* Тест выполнения функции с ошибкой и указанием файла логов.
* Тест выполнения функции с ошибкой без указания файла (вывод в консоль).
* Тест функции с именованными аргументами.
* Тест функции, возвращающей None.

### 11. Проверка кода для чтения кода в формате JSON-файл read_transactions с использованием Mock и patch.

* Тест успешное чтение валидного JSON-файла со списком транзакций.
* Тест чтение несуществующего файла возвращает пустой список.
* Тест чтение пустого JSON-файла возвращает пустой список.
* Тест чтение JSON с пустым списком возвращает пустой список.
* Тест чтение JSON с объектом (не списком) возвращает пустой список.
* Тест чтение файла с невалидным JSON возвращает пустой список.
* Тест обработка ошибки доступа к файлу возвращает пустой список.

### 12. Проверка кода конвертирует сумму транзакции в рубли convert_to_rubles с использованием Mock и patch.

* Тест если валюта уже рубли — возвращается исходная сумма.
* Тест конвертация USD в рубли через маркированный вызов API.
* Тест конвертация EUR в рубли через маркированный вызов API.
* Тест при ошибке API возвращается исходная сумма.
* Тест при тайм-ауте API возвращается исходная сумма.
* Тест обработка транзакции без ключа operationAmount.
* Тест обработка невалидной строки суммы.
* Тест неизвестный код валюты возвращает исходную сумму.
* Тест если API вернул ответ без ключа 'result' — используется исходная сумма.

### 13. Проверка кода _extract_date с использованием Mock и patch.

* Тест при невалидном формате даты используется дата по умолчанию.

### 14. Проверка кода read_transactions_from_csv с использованием Mock и patch.

* Тест ожидаемый результат (контракт функции).
* Тест изоляция внешнего ввода-вывода и парсера.
* Тест Вызов тестируемой функции.
* Тест Проверки (Assertions).
* * Проверяем, что функция вернула именно те данные, которые мы "скормили" парсеру.
* * Проверяем, что DictReader был вызван ровно один раз.
* * Проверяем, что open был вызван с правильным путём, режимом и кодировкой.

### 15. Проверка кода read_transactions_from_excel с использованием Mock и patch.

* Тест ожидаемый результат (контракт функции).
* Тест изоляция чтения файла.
* Тест вызов тестируемой функции.
* Тест проверки.
* * Убеждаемся, что pd.read_excel был вызван с правильным путём и ровно 1 раз.

### 17. Проверка кода read_transactions_from_excel с использованием pytest

* Тест обработки отсутствующей зависимости openpyxl.
* Тест обработки отсутствующего файла на диске.
* Тест обработки непредвиденных ошибок парсинга (повреждённый файл, битые структуры).

### 18. Проверка кода process_bank_search с использованием pytest и с использованием Mock и patch

* Тест проверяет разные сценарии поиска.
* Тест пустой запрос должен вернуть исходный список без изменений.
* Тест транзакции без description или с None не должны вызывать ошибку.
* Тест Пример использования patch/Mock: проверяем, что re.compile вызывается.
    с экранированной строкой и флагом re.IGNORECASE.
* * Патчим re.compile именно в пространстве имён модуля filters.
* * Проверяем аргументы вызова re.compile.

### 19. Проверка кода process_bank_operations с использованием pytest и с использованием Mock и patch

* Тест проверяет корректность подсчёта для разных наборов категорий.
* Тест порядок ключей в результате должен строго совпадать с порядком переданных категорий.
* Тест Пустые списки на входе должны вернуть пустой словарь.
* Тест Отсутствие ключа description не вызывает ошибку (dict.get вернёт None).
* Тест Демонстрация patch/Mock: проверяем, что Counter инициализируется ровно один раз
    и получает итерируемый аргумент (генератор).
* * Настраиваем поведение __getitem__: вернёт 1 для "Test", 0 для остальных.
* * Патчим Counter именно в пространстве имён bank_processor.
* * Проверяем, что был передан ровно один позиционный аргумент.
* * Убедимся, что аргумент является итерируемым объектом (генератором/итератором).

### 19. Проверка кода main с использованием pytest и с использованием Mock и patch и при помощи библиотеки random
 
* ФИКСТУРЫ
* Тест: Генерация случайных транзакций во вложенной JSON-структуре.
    ЧТО ПРОВЕРЯЕТ: Способность приложения корректно работать с вложенными данными
    (как при загрузке из JSON или API).
    ОСОБЕННОСТЬ: Гарантированно добавляет ровно одну рублёвую транзакцию,
    чтобы позже проверить логику фильтрации по алиасам рубля.
* Тест: Генерация случайных транзакций в плоской структуре.
    ЧТО ПРОВЕРЯЕТ: Корректность обработки данных из CSV/XLSX,
    где поля (amount, currency_name) находятся на верхнем уровне,
    а не вложены в operationAmount.
* Тест: Изоляция приложения от внешних зависимостей.
    ЧТО ПРОВЕРЯЕТ: Заменяет реальные функции чтения файлов, аналитики,
    поиска, форматирования даты и маскирования карт на mock-объекты.
    ПОЧЕМУ ЭТО НУЖНО: Тесты становятся детерминированными, не зависят
    от файловой системы, библиотек и рандома внутри main.py.
    Позволяет проверять, какие функции были вызваны и с какими аргументами.
* МОДУЛЬНЫЕ ТЕСТЫ
* Тест: Извлечение суммы и валюты из JSON-структуры.
    ЧТО ПРОВЕРЯЕТ: Функция корректно парсит вложенный ключ operationAmount,
    преобразует строку с суммой в float и возвращает точное название валюты.
* Тест: Извлечение суммы и валюты из плоской структуры.
    ЧТО ПРОВЕРЯЕТ: То же, что и выше, но для данных CSV/XLSX,
    где amount и currency_name лежат на верхнем уровне словаря.
* Тест: Форматирование суммы для вывода пользователю.
    ЧТО ПРОВЕРЯЕТ: Наличие пробела-разделителя тысяч (например, "1 234,56")
    и корректное сохранение цифр в строке.
* Тест: Фильтрация транзакций по валюте с поддержкой синонимов рубля.
    ЧТО ПРОВЕРЯЕТ:
    1. Точное совпадение для обычных валют (например, "Euro").
    2. Учёт алиасов рубля: "рубль", "RUB", "руб.", "rouble" и т.д.
    3. Динамический подсчёт expected_count гарантирует, что тест
       не будет "flaky" из-за случайной генерации данных.
* Тест: Сбор уникальных валют из смешанного списка транзакций.
    ЧТО ПРОВЕРЯЕТ:
    1. Удаление дубликатов.
    2. Строгая сортировка результата по алфавиту.
ИНТЕГРАЦИОННЫЕ ТЕСТЫ (main flow)
* Тест: Полный консольный сценарий выбора валюты.
    ЧТО ПРОВЕРЯЕТ:
    - Пользователь выбирает только рубли → выводится "только рублевые".
    - Пользователь выбирает другую валюту → вводит её, выводится "Применен фильтр...".
    - Пользователь отказывается от фильтра → выводится общая статистика.
    - Захватывает stdout через capsys и проверяет ожидаемые фразы.
* Тест: Корректный выбор функции чтения файла по вводу пользователя.
    ЧТО ПРОВЕРЯЕТ: При вводе "1" вызывается только JSON-ридер,
    "2" → CSV-ридер, "3" → XLSX-ридер. assert_called_once() гарантирует,
    что лишние функции чтения не срабатывают.
* ТЕСТЫ ОБРАБОТКИ ОШИБОК
* Тест: Обработка отсутствия файла.
    ЧТО ПРОВЕРЯЕТ: При выбросе FileNotFoundError обёртка не падает,
    а возвращает пустой список [].
* Тест: Обработка отсутствия библиотеки openpyxl.
    ЧТО ПРОВЕРЯЕТ: При ImportError (нет модуля xlsx) функция
    корректно ловит исключение и возвращает [].
* Тест: Обработка неподдерживаемого формата.
    ЧТО ПРОВЕРЯЕТ: Передача "xml" или другого неизвестного типа
    должна вернуть [], а не вызвать KeyError или crash.
* Тест: Загрузка пустого файла.
    ЧТО ПРОВЕРЯЕТ: Если ридер вернул [], приложение должно вывести
    сообщение об ошибке и корректно завершить работу, не переходя к фильтрам.
* Тест: Фильтр отсеивает все транзакции.
    ЧТО ПРОВЕРЯЕТ: Если данные есть, но ни одна не прошла фильтр (например,
    ищем RUB, а в файле только USD), приложение должно вывести
    "Не найдено ни одной транзакции" и не упасть при попытке форматирования.


